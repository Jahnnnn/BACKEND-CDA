package com.cda.repositories.Carnet;

import com.cda.models.Carnet;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CarnetRepository extends JpaRepository<Carnet, Long> {
}
